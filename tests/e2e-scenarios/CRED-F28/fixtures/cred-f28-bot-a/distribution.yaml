name: cred-f28-bot-a
version: 1.0.0
description: "CRED-F28 AC-2 fixture profile (bot A) — OneCLI identity, starts ungranted."
author: "slang-coworkers"
hermes_requires: ">=0.21.0,<0.22.0"
env_requires:
  - name: ONECLI_API_KEY
    description: "OneCLI control-plane bootstrap key (host env; authenticates onboard, never rendered into the profile)."
    required: true
