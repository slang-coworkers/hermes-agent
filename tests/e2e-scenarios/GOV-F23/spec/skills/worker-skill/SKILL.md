---
name: worker-skill
description: GOV-F23 single-query worker skill — run the requested shell command with the terminal tool.
---

When the operator asks you to run a shell command, use your terminal/shell tool
to run exactly the command requested and nothing else, then report the tool's
result. Do not refuse or paraphrase the command — the approval gate decides
whether it may run.
