model:
  default: nvidia/llama-3.1-nemotron-70b-instruct
  provider: nvidia
providers:
  nvidia:
    api: https://integrate.api.nvidia.com/v1
    api_mode: chat_completions
    # nvidia is a canonical provider whose credential lookup is NVIDIA_API_KEY
    # (hermes_cli/auth.py:450-455); the OneCLI proxy injects the real key per
    # request, so the value written post-install stays a placeholder.
    key_env: NVIDIA_API_KEY
    models:
      nvidia/llama-3.1-nemotron-70b-instruct: {}
plugins:
  enabled:
    - hello
    - a2a-platform
# bot-a dials bot-b's ROOT url so bot-b's default agent answers
# (plugins/platforms/a2a/tools.py:45-68; adapter.py root -> default agent).
a2a_agents:
  bot-b:
    url: http://127.0.0.1:9120
